<header class="navbar">
  <div class="nav-container">
    <div class="nav-logo">
      <img src="/favicon.svg" alt="Logo" class="logo-img" />
      <span class="logo-text">Taiwan<span class="dot-md">.sst</span></span>
    </div>

    <div class="hamburger-icon" id="menuToggle">
      <span></span><span></span><span></span>
    </div>

    <nav class="nav-menu" id="navMenu">
      <div class="nav-links">
        <a href="/year/2021" class="nav-item">2021 (反聖嬰)</a>
        <a href="/year/2015" class="nav-item">2015 (強聖嬰)</a>
        <a href="/year/2016" class="nav-item">2016 (聖嬰)</a>
        <a href="/year/2023" class="nav-item">2023 (聖嬰)</a>
      </div>
    </nav>

    <div class="nav-actions">
      <button class="btn-icon">🔍</button>
      <div class="lang-switch">
        <button class="lang-btn active">中文</button>
        <button class="lang-btn">EN</button>
      </div>
    </div>
  </div>
</header>
<style> 
 /* 分頁切換按鈕 */
 .navbar {
    background: white;
    border-bottom: 1px solid #e5e7eb;
    position: sticky;
    top: 0;
    z-index: 1000;
    padding: 10px 20px;
  }
  .nav-container {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .nav-logo { font-size: 1.5rem; font-weight: bold; display: flex; align-items: center; gap: 8px; }
  .dot-md { color: #3b82f6; }
  .nav-links { display: flex; gap: 15px; }
  .nav-item {
    text-decoration: none;
    color: #4b5563;
    padding: 8px 12px;
    border-radius: 6px;
    transition: 0.2s;
  }
  .nav-item:hover { background: #f3f4f6; }
        .page-tabs { display: flex; justify-content: center; margin-bottom: 20px; border-bottom: 2px solid #eee; padding-bottom: 10px; }
        .tab-btn { padding: 10px 25px; cursor: pointer; border: none; background: #f0f0f0; margin: 0 5px; border-radius: 5px; font-weight: bold; transition: 0.3s; }
        .tab-btn.active { background: var(--accent-color); color: white; }
       .hamburger-icon {display: none; flex-direction: column;gap: 5px;cursor: pointer;padding: 10px;width: fit-content;}
        .hamburger-icon span {width: 25px; height: 3px;background: #1a2a3a;border-radius: 2px;}
        .tab-group {display: flex; gap: 10px;}
@media screen and (max-width: 1200px) {
     /* 當漢堡被點擊時開啟 */
    .tab-group.active {
        display: flex;
        transform: translateX(-15px);
    }

    .tab-btn { width: 100%; text-align: left; }
    }
</style>